const config = {
  ENDPOINT: '/transactions', // change this to your base api url
  STATUS: {
    initiated: 'INITIATED',
    completed: 'COMPLETED',
    failed: 'FAILED'
  }
};

module.exports = config;
